{
  "name": "telegram-focus-bot",
  "version": "1.0.0",
  "description": "MVP Telegram bot for focus tracking with natural language processing and estimation accuracy",
  "main": "api/webhook.js",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for serverless functions'",
    "deploy": "vercel --prod"
  },
  "keywords": [
    "telegram",
    "bot",
    "focus",
    "productivity",
    "estimation",
    "tracking"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "@upstash/redis": "^1.25.1",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
